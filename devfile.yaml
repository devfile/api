apiVersion: 1.0.0
metadata:
  name: devworkspace-api
components:
  - id: ms-vscode/go/latest
    memoryLimit: 512Mi
    preferences:
      go.lintTool: golangci-lint
      go.lintFlags: '--fast'
    type: chePlugin
    alias: go-plugin
  - id: redhat/vscode-yaml/latest
    type: chePlugin
  - mountSources: true
    command:
      - /bin/sh
    args:
      - '-c'
      - >-
        sleep 10 ;  sed -e
        's:/schemas/:/che-incubator-devworkspace-api/schemas/:g'
        /projects/che-incubator-devworkspace-api/.theia/settings.json >
        /projects/.theia/settings.json; sleep infinity
    memoryLimit: 56Mi
    type: dockerimage
    image: busybox
