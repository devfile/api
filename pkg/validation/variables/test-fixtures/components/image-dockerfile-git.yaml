imageName: "myimage:{{ tag }}"
dockerfile:
  git:
    gitLocation: "{{ uri }}/Dockerfile"
    checkoutFrom:
      revision: "{{foo}}"
      remote: "{{bar}}"
    remotes:
      "foo": "{{bar}}"
      "{{foo}}{{bar}}": "{{ bar }}{{  foo }}"
  buildContext: /{{foo}}/{{bar}}
  rootRequired: true
