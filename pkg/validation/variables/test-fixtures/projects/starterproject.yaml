# Variables are defined in test-fixtures/variables/variables-referenced.yaml
name: starterproject1
zip:
  location: "/{{foo}}"
description: "{{ foo }}{{ bar }} is not {{bar}}{{foo}}"
subDir: "/{{foo}}"
