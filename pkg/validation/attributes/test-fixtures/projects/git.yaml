# Attributes are defined in test-fixtures/attributes/attributes-referenced.yaml
git:
  checkoutFrom:
    revision: "{{foo}}"
    remote: "{{bar}}"
  remotes:
    "foo": "{{bar}}"
    "{{foo}}{{bar}}": "{{bar}}{{foo}}"