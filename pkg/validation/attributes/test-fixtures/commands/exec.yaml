# Attributes are defined in test-fixtures/attributes/attributes-referenced.yaml
component: "{{bar}}"
commandLine: tail -f {{devnull}}
workingDir: "{{foo}}"
label: "{{version}}"
env:
  - name: "{{foo}}"
    value: "{{bar}}"
