attributes:
  devnull: /dev/null
components:
- name: component1
  container:
    image: image
    env:
      - name: BAR
        value: "{{foo}}"
    command:
      - tail
      - -f
      - "{{devnull}}"
