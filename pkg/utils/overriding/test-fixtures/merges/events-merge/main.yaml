parent:
  uri: "anyParent"
components:
  - plugin:
      uri: "aCustomLocation"
    name: "theOnlyPlugin"
events:
  preStart:
    - "preStartFromMainContent"
  preStop:
    - "preStopFromMainContent"
  postStart:
    - "postStartFromMainContent"
  postStop:
    - "postStopFromMainContent"
